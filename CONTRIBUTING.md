# Contributing

Keep network work in repositories/services, avoid force unwraps, add unit tests for API changes, and never commit credentials, tokens, certificates, or provisioning profiles. API paths require evidence in `docs/API-MAP.md` before use.


# Validation record

* 2026-08-26: public API probe `GET https://api-s.anixsekai.com/release/101?extended_mode=true` returned HTTP 200 and `code: 0`.
* 2026-08-26: AnixartJS and AnixAPI source was independently cloned at depth 1 and endpoint paths/parameters were compared; results are recorded in `API-MAP.md`.
* Windows has no Swift or Xcode toolchain in this workspace, so native compilation cannot be claimed locally. `.github/workflows/ios.yml` is the required macOS build/test gate. The first GitHub Actions run must be treated as the compilation verification.
* 2026-08-26: Player chain fully rewritten after auditing anixapi source for real API types — `Episode.source` is `SourceReference` (polymorphic `Int | Object`), `SourceReference` dual-decode implemented. `Dubber` extended with `icon`, `workers`, `isSub`, `pinned`, `viewCount`. `VideoFormat` enum added with `detect()` for HLS/MP4/iframe/redirect detection.
* 2026-08-26: Watch/unwatch endpoints corrected to POST (was incorrectly GET). Added `markSourceWatched`/`markSourceUnwatched` for source-level endpoints. Both single and batch mark-as-watched paths now implemented.
* 2026-08-26: `PlayerFlowView` rewritten — `NativePlayerView` wraps `AVPlayerViewController` via `UIViewControllerRepresentable`. `PlayerCoordinatorViewController` delegates control. `PlayerCoordinator` handles progress persistence, server-side history sync, and auto-mark-watched at 90%. URL resolution with redirect-follow implemented. iframe sources show copy-link fallback with explanatory UI.
* 2026-08-26: Frame-by-frame stepping added via `,` (back) / `.` (forward) keyboard shortcuts at 1/30s timescale. Arrow keys for 5s seek, space for play/pause, Cmd+arrows for episode switching.
* 2026-08-26: Thumbnail preview infrastructure added — `ThumbnailPreviewView` (UIView) with `AVAssetImageGenerator` pipeline wired to `PlayerCoordinatorViewController`. Actual thumbnails depend on server-side HLS thumbnail track availability (documented limitation).
* 2026-08-26: Test suite expanded to 30 tests — covers `VideoFormat` detection, `Episode.isPlayable`, `SourceReference` dual-decode (int vs object), `Dubber` optional fields, full response chain decoding (`EpisodesResponse`, `SourcesResponse`, `EpisodeTypesResponse`).
* 2026-08-26: Pre-CI audit found and fixed 4 issues: (1) `stepBackward` used `CMTimeMaximum` instead of `CMTimeSubtract` — frame-step backward was broken, never going backward; (2) removed unused `Set<AnyCancellable>` requiring `import Combine` — compile error; (3) `WatchDynamic` missing `Identifiable` conformance for `ForEach` in `ProfileView` — compile error; (4) `rewind`/`forward` now clamp to zero/duration bounds.
* 2026-08-26: Thumbnail preview: `AVAssetImageGenerator` infrastructure wired but `showThumbnail` not invoked from any UI gesture. For HLS streams, `AVAssetImageGenerator` can extract frames but accuracy depends on keyframe intervals in the stream. Limitation documented — real implementation requires connecting to timeline scrub gestures.
* 2026-08-26: CI build fixes (Xcode 16.4 / Swift 6): (1) removed `playerViewControllerShouldDismiss(_:)` — marked unavailable in current AVKit SDK; (2) rewrote `SearchView.swift` with proper ViewBuilder formatting — semicolons inside result builder closures broke the compiler transform; (3) removed `TokenProviding` conformance from `AuthManager` — `@MainActor` class cannot satisfy nonisolated protocol requirement; `KeychainStore` already conforms and serves as `APIClient` token provider; (4) created valid `AppIcon.appiconset` with placeholder PNG and correct `Contents.json` referencing it.

